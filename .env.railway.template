# ─────────────────────────────────────────────────────────────
# Waslney — Railway Environment Variables
# Copy these into Railway → your service → Variables tab
# DO NOT commit real values to GitHub
# ─────────────────────────────────────────────────────────────

# ── Database (your existing Hostinger MySQL) ──────────────────
DB_HOST=your-hostinger-db-host        # e.g. srv1234.hstgr.io
DB_USER=u946447529_Moatasem
DB_PASS=your_db_password_here
DB_NAME=u946447529_Wasalney
DB_PORT=3306

# ── Auth ─────────────────────────────────────────────────────
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_A_STRONG_RANDOM_SECRET
JWT_EXPIRES=7d

# ── Email (Hostinger SMTP) ───────────────────────────────────
MAIL_USER=support@waslney.com
MAIL_PASS=your_email_password_here

# ── Server ───────────────────────────────────────────────────
# Railway sets PORT automatically — do NOT hardcode it
# PORT is already read from env in server.js ✓
NODE_ENV=production
