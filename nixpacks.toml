[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npx --yes pnpm@10 install --no-frozen-lockfile"]

[phases.build]
cmds = ["npx pnpm@10 -r --filter '!@workspace/mockup-sandbox' --if-present run build"]
